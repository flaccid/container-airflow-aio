# container-airflow-aio

Apache Airflow All-In-One container solution.

## Usage

```
make help
```

Example:

```
docker run \
    --name airflow-aio \
    -it \
    --rm \
    -p 8080:8080 \
    flaccid/airflow-aio
```
